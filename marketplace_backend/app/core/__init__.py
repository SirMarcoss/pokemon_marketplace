# This file is intentionally left empty.
# Its presence tells Python that this directory is a package,
# making it possible to import modules from it.
# Without this file, Python would treat this folder as a plain directory,
# and imports like 'from app.models.base import Base' would fail.